Keep the current Task #11 scope and confirmed audit findings, but add these implementation guardrails:

Use a cost-conscious, minimal-change approach. Prioritize fixing the confirmed security, reliability, rate-limit, API-efficiency, and testing issues identified by the audit. Do not spend significant effort on speculative refactoring, theoretical hardening, or unrelated improvements.

Before changing anything, verify whether an existing implementation already adequately addresses an issue. If it does, leave it unchanged.

Reuse the existing architecture, dependencies, utilities, authentication middleware, validation, generated API clients, logging, and testing patterns wherever possible.

Do not introduce Redis, queues, workers, new databases, new frameworks, or unnecessary infrastructure just for Phase 9.

For rate limiting, use the simplest practical server-side approach that fits the existing application. Keep limits reasonable for development and normal usage.

For testing, prefer lightweight executable checks using the repository's existing tooling. Do not install a new testing framework unless it is genuinely necessary to verify a confirmed security or reliability issue.

Do not rewrite working Phase 1–8 functionality merely for theoretical improvements.

Preserve:
- Firebase Authentication
- Firestore
- Gemini
- Qdrant
- embeddings
- memory extraction/retrieval/CRUD
- agent/tool calling
- feedback/adaptation
- conversation-title editing
- responsive/mobile navigation
- Markdown rendering
- existing API contracts wherever possible

Pay particular attention to the confirmed audit findings:
- missing server-side rate/concurrency protection
- unrestricted CORS
- request parsing and malformed-input handling
- deterministic bounded conversation history
- message ownership filtering
- whitespace-only conversation titles
- safe diagnostic logging
- provider/persistence failure handling
- missing automated boundary checks
- dependency vulnerabilities, but only apply safe compatible upgrades where appropriate

Do not make dependency upgrades solely because a scanner reports an issue if the upgrade would risk breaking the application. Report such issues instead if they cannot be safely remediated.

Do not claim a vulnerability is fixed or a test has passed unless it was actually verified.

Run the actual typechecks, production builds, tests/checks, and relevant security scans after the changes.

If a requested verification cannot be performed in the available environment, explicitly report:
"Not verified yet."

STOP after Phase 9. Do not begin Phase 10.